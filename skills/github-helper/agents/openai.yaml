interface:
  display_name: "GitHub Helper"
  short_description: "Manage local GitHub repos and search issues/PRs fast."
  default_prompt: "Use $github-helper to locate repositories, sync local clones, and inspect issues or pull requests."
